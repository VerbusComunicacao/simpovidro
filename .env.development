POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=local_user
POSTGRES_DB=local_db
POSTGRES_PASSWORD=local_password
DATABASE_URL=postgress://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:$POSTGRES_PORT/$POSTGRES_DB

#Sitema de e-mail principal
EMAIL_SMTP_HOST=localhost
EMAIL_SMTP_PORT=1025
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=
EMAIL_SMTP_SECURE=false

EMAIL_HTTP_HOST=localhost
EMAIL_HTTP_PORT=1080

#Sistema de e-mail de backup em caso de falha do primeiro
FALLBACK_EMAIL_SMTP_HOST=
FALLBACK_EMAIL_SMTP_PORT=
FALLBACK_EMAIL_SMTP_USER=
FALLBACK_EMAIL_SMTP_PASSWORD=
FALLBACK_EMAIL_SMTP_SECURE=

# Analytics Integration
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_CLARITY_ID=